# Esquire Disassembly

This repository contains a living disassembly and annotation of the Esquire 9.04 scheduler, built for the Amiga platform. The goal is to document system behavior, preserve historical software, and enable small targeted fixes through commented assembly.

## Repository Layout
- `src/Prevue.asm` – Root include that pulls in every module. Use it as the entry point for builds.
- `src/modules/groups/` – Feature modules grouped into lettered folders (`a`, `b`, `c`, `d`, etc.) based on shared jump-table/export patterns; use these when tracking related entry stubs.
- `src/modules/submodules/` – Provisional utility chunks split out during the re-org; filenames may remain `unknown*.s` until their roles are confirmed.
- `src/data/` – Display tables, highlight presets, string resources, and lookup maps shared across modules.
- `src/subroutines/` and `src/interrupts/` – Common helpers and interrupt handlers referenced from multiple modules.
- `src/decomp/` – Optional decomp scaffold for incremental asm→C conversion experiments. The primary restored-source lane lives in `src/decomp/sas_c/`, while `src/decomp/c/replacements/` holds GCC trial candidates for targets that have not yet been moved into the SAS/C lane.
- `assets/` – External requirements: Workbench ROM in `kickstart/` and the extracted HDD image in `disks/prevue/`.
- `docs/` – Focused reference notes for recovered file formats and cross-module mapping docs.
- `build/` – Disposable build output regenerated by the scripts.
- `build.sh`, `test-hash.sh` – Helper scripts for assembling and verifying the image with the vasm toolchain.

## Pre-Requisites
- Place the Amiga Workbench 2.04 ROM at `assets/kickstart/v2.04.rom`.
- Download and extract the [Esquire 9.04 HDD image](https://park-city.club/~frix/prevue/Prevue.zip) into `assets/disks/prevue/`.
- Install [vasm](https://sun.hasenbraten.de/vasm/) (m68k, `mot` syntax) and update any hard-coded paths in the scripts to match your setup.
  - I've used [vbcc_installer](https://github.com/robertkist/vbcc_installer) to get vasm, vlink, and vbcc installed. Clone the repository, run `make download` and then `make setup` with all default options to build those binaries.
- Install [m68k-amigaos-gcc](https://github.com/AmigaPorts/m68k-amigaos-gcc) if you intend to do any decompilation work.
- Optional: Install the [Amiga Assembly](https://marketplace.visualstudio.com/items?itemName=prb28.amiga-assembly) VS Code extension for improved syntax support.

## Build & Verify
1. Ensure the vasm 68k binaries are installed and update their paths inside `build.sh` and `test-hash.sh` if your setup differs.
2. Make the helper scripts executable: `chmod +x build.sh test-hash.sh`.
3. Assemble the project with `./build.sh`. By default this writes `ESQ` to `~/Downloads/Prevue/`; edit the script to change the destination.
4. Confirm determinism with `./test-hash.sh`. A matching build prints `6bd4760d1cf0706297ef169461ed0d7b7f0b079110a78e34d89223499e7c2fa2` as the SHA-256 digest.

For targeted experiments you can run vasm directly, for example:
```bash
~/Downloads/vasm/vasmm68k_mot -Fhunkexe -linedebug -o build/ESQ src/Prevue.asm
```
Keep any generated binaries out of version control.

## Development Workflow
- Alignment/padding bytes found after jump tables are likely compiler artifacts that mark original source/object boundaries; treat them as file-end markers unless proven otherwise.
  - Heuristic only: padding can appear mid-file for alignment, and jump tables are suggestive but not definitive boundaries.
- Some jump tables appear shared across multiple files, likely due to linker layout/segment reuse; grouping under `src/modules/groups/` is a working hypothesis to reflect those shared entry stubs, not a definitive source boundary.
- The current decomp goal is “mostly equivalent” C for the original assembly, not source beautification or optimization. Preserve control flow, stack shape, and data layout unless a change is explicitly part of the reverse-engineering work.
- `src/decomp/sas_c/` is the restored SAS/C-oriented source tree. Running `./sc-build-with-dis.sh <file>.c` against a file in that directory produces matching `.o` and `.dis` artifacts beside the source for local disassembly comparison.
- Existing work in `src/decomp/sas_c/` can be used as the style and structure reference for new restored functions. Many helper/compare lanes already compile there successfully.
- When a restored SAS/C source needs an AmigaOS system struct that already exists under `/Users/RJ/Downloads/SAS-C-hdd/sc/include`, prefer including the canonical SAS/C header over maintaining a local duplicate struct declaration. `graphics/rastport.h` is the current baseline example for `struct RastPort`.
- Current carry-forward note (March 13, 2026): the restored SAS/C lane now already uses canonical SAS/C headers in many places for `RastPort`, `TextFont`, `BitMap`, `MinList`, `MsgPort`, `Message`, `IOStdReq`, and `Library`. Treat those headers as the default choice, not the exception.
- Current exception note (March 13, 2026): `src/decomp/sas_c/cleanup_draw_grid_time_banner.c` and `src/decomp/sas_c/render_short_month_short_day_of_week_day.c` still keep local overlay structs because they currently rely on nonstandard cached/overlaid fields rather than plain header layouts.
- Current repo state (re-validated locally on March 13, 2026): the maintained SAS/C lane is green. Targeted `run_sasc_core_sweep.sh --strict --filter ...` reruns for the previously reported blocker set (`string_append_at_null`, `newgrid_init_grid_resources`, `cleanup_build_and_render_aligned_status_banner`, `cleanup_render_aligned_status_screen`, and `diskio1_dump_default_coi_info_block`) all currently resolve to zero-byte semantic diffs, and `compare_sasc_diskio1_dump_default_coi_info_block_trial.sh` now compiles cleanly in this checkout.
- Current triage state (March 13, 2026, later re-validated): do not restart from the older maintained-sweep blocker list. The maintained `run_sasc_core_sweep.sh` set is green in this checkout, and the previously noted out-of-sweep semantic diffs in `disptext_is_current_line_last`, `disptext_is_last_line_selected`, and `newgrid_update_grid_state` also now resolve to zero-byte semantic diffs after updating the SAS/C semantic filters to accept normalized `MOVEQ #-1,D0` booleanization and direct `DISPTEXT_ComputeVisibleLineCount` calls. Re-triage broader mismatch work from current sweep output rather than this older short list.
- Additional triage note (March 13, 2026): the strengthened `src/decomp/scripts/list_missing_sasc_non_jmptbl_exports.py` helper now checks for real SAS/C function definitions, not just exact-word hits from `extern` declarations or callers. In this checkout it still prints no rows, so do not assume a non-wrapper GCC trial still lacks a restored SAS/C definition without checking first.
- Additional tooling note (March 13, 2026): `src/decomp/scripts/report_hybrid_module_promotion_coverage.py` now normalizes compare lanes that still cite passthrough files under `src/decomp/replacements/` back to the original `src/modules/...` path before checking `src/decomp/replacements.map`. Treat the normalized path as authoritative when triaging hybrid module coverage.
- Additional tooling note (March 14, 2026): `src/decomp/scripts/report_passthrough_integration_candidates.py` now filters the mapped-module candidate set down to fully covered module boundaries whose replacement file still only `include`s the canonical asm module. Use its `ready` rows as the default queue for object-level hybrid integration work after the strict SAS/C sweep is green, add `--details` when you need the covered export names plus backing `compare_sasc_*` scripts for a chosen module, and add `--verify` when you need the report to rerun those compare lanes and distinguish semantic-only coverage from exact/raw-diff-clean candidates.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/a/app.s` is now seeded in `src/decomp/replacements.map` alongside the already-mapped APP2/BRUSH/BEVEL neighbors. The replacement boundary still includes the original asm module verbatim for now, but the hybrid include graph no longer needs another root-level edit when that serial/control helper cluster starts moving behind restored C objects.
- Current hybrid-integration note (March 14, 2026): `modules/groups/_main/a/a.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries the `_main` startup/shutdown helper module body directly, so the `ESQ_StartupEntry` / `ESQ_ReturnWithStackCode` / `ESQ_ShutdownAndReturn` foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 14, 2026): `modules/groups/a/a/app3.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries `ESQ_InvokeGcommandInit`, `ESQ_SupervisorColdReboot`, and `ESQ_TryRomWriteTest` directly, so this reboot/init helper foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 14, 2026): `modules/groups/a/d/cleanup3.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries the CLEANUP3 aligned-status banner/screen helper module body directly, so this aligned-status foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/s/gcommand.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the main GCOMMAND parser/template helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/t/gcommand2.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the GCOMMAND2 helper foothold (`GCOMMAND_CopyGfxToWorkIfAvailable`, `GCOMMAND_FindPathSeparator`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/u/gcommand3.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the GCOMMAND3 banner/highlight helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/u/gcommand4.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the GCOMMAND4 brush-result helper foothold (`GCOMMAND_SaveBrushResult`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 14, 2026): `modules/groups/a/v/gcommand5.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries `GCOMMAND_ProcessCtrlCommand` directly, so this control-command helper foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 14, 2026): `modules/groups/a/v/kybd.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries `KYBD_InitializeInputDevices` directly, so this input-device initialization foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/h/diskio2.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the DISKIO2 data-file transfer, sync, and ini/oinfo helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/l/ed3.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the ED3 text/cursor and line/page draw-helper foothold as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 14, 2026): `modules/groups/a/m/esq.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries `ESQ_MainInitAndRun` directly, so this main startup/init foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/a/i/displib.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the DISPLIB text/layout helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/a/n/esqfunc.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the ESQFUNC status/clock/UI-service helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/a/x/ladfunc2.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the LADFUNC2 escaped-string helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/a/w/ladfunc.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the LADFUNC text-ad/highlight helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/a/y/locavail.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the LOCAVAIL local-availability filter helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/a/z/locavail2.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the LOCAVAIL2 intuition override helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/parseini.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the PARSEINI config/weather/font helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/b/a/parseini2.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the PARSEINI2 clock/date/RTC helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/p_type.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the P_TYPE promo-id/group-list helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/script.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the SCRIPT buffer allocation/deallocation/token-index helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/b/a/tliba2.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the TLIBA2 broadcast-window/time-parse helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/tliba1.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the TLIBA1 formatted-text/clock-format helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/tliba3.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the TLIBA3 view-mode/pattern-table helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/newgrid1.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the NEWGRID1 grid drawing/scan helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/textdisp.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the TEXTDISP source-config/selection helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/textdisp2.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the TEXTDISP2 preview/display-state helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/groups/b/a/script4.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the SCRIPT4 banner-char/inset/highlight helper cluster as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/groups/b/a/textdisp3.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the TEXTDISP3 text/layout helper cluster as integration-ready for future object-level replacement work, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/submodules/unknown10.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the UNKNOWN10 format/parse/open helper cluster as integration-ready for future object-level replacement work where the restored SAS/C foothold already exists (`FORMAT_U32ToHexString`, `PARSE_ReadSignedLong`, `PARSE_ReadSignedLong_NoBranch`, `HANDLE_OpenEntryWithFlags`, `UNKNOWN10_PrintfPutcToBuffer`, and `WDISP_SPrintf`), even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/submodules/unknown22.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the UNKNOWN22 arithmetic/msgport helper cluster (`ALLOCATE_AllocAndInitializeIOStdReq`, `DOS_CloseWithSignalCheck`, `MATH_DivS32`, `MATH_DivU32`, `MATH_Mulu32`, and `SIGNAL_CreateMsgPortWithSignal`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/submodules/unknown24.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the UNKNOWN24 memlist/signed-long helper cluster (`MEMLIST_AllocTracked`, `MEMLIST_FreeAll`, `PARSE_ReadSignedLongSkipClass3`, and `PARSE_ReadSignedLongSkipClass3_Alt`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/submodules/unknown29.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the UNKNOWN29 command-line/startup helper module (`ESQ_ParseCommandLineAndRun`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/submodules/unknown.s`, `modules/submodules/unknown2a.s`, and `modules/submodules/unknown30.s` are now seeded in `src/decomp/replacements.map` as passthrough hybrid boundaries. Treat the Unknown protocol/status-parse helper cluster (`ESQPROTO_CopyLabelToGlobal`, `ESQPROTO_ParseDigitLabelAndDisplay`, `ESQPROTO_VerifyChecksumAndParseList`, `ESQPROTO_VerifyChecksumAndParseRecord`, `UNKNOWN_ParseListAndUpdateEntries`, `UNKNOWN_ParseRecordAndUpdateDisplay`), the Unknown2A formatter foothold (`FORMAT_RawDoFmtWithScratchBuffer`, `UNKNOWN2A_Stub0`), and the Unknown30 Exec helper foothold (`EXEC_CallVector348`) as integration-ready at the module boundary, even though the replacement files still include the canonical asm modules verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/submodules/unknown32.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the UNKNOWN32 handle-close/return helper module (`HANDLE_CloseAllAndReturnWithCode`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 13, 2026): `modules/submodules/unknown34.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the UNKNOWN34 list/memory/read helper cluster (`LIST_InitHeader`, `MEM_Move`, and `DOS_ReadByIndex`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Current hybrid-integration note (March 14, 2026): `modules/submodules/unknown38.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries `SIGNAL_PollAndDispatch` directly, so this signal/exit helper foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 14, 2026): `modules/submodules/unknown11.s` is now a direct object-level hybrid replacement rather than a passthrough include. The replacement file carries `DOS_SeekByIndex` directly, so this DOS seek helper foothold is no longer waiting on a first module-body integration pass.
- Current hybrid-integration note (March 13, 2026, later re-validated): `modules/submodules/unknown7.s` is now seeded in `src/decomp/replacements.map` as a passthrough hybrid boundary. Treat the UNKNOWN7 string delimiter/search helper cluster (`STR_CopyUntilAnyDelimN`, `STR_FindChar`, `STR_FindCharPtr`, `STR_FindAnyCharInSet`, `STR_FindAnyCharPtr`, `STR_SkipClass3Chars`) as integration-ready at the module boundary, even though the replacement file still includes the canonical asm module verbatim for now.
- Remaining decomp work is not just “write more assembly comments”: the scope is to cover the root `src/*.s` files, `src/Prevue.asm`, and all files under `src/interrupts/`, `src/data/`, and `src/modules/` recursively.
- In practice, the next decomp steps tend to fall into two buckets:
  - tighten or split existing `src/decomp/sas_c/` files when a function already exists there but needs a better match;
  - add new `src/decomp/sas_c/*.c` ports for targets that currently exist only as GCC trial/replacement files under `src/decomp/c/replacements/`.
- When triaging a candidate, prefer `src/decomp/scripts/run_sasc_core_sweep.sh --filter <substring>` or the target-specific `compare_sasc_*` script before reading large asm slices by hand. If the semantic diff is empty, move on unless you specifically need a better non-semantic codegen match.
- Treat compiler-emitted jump-table wrapper exports as low priority unless they are required glue for a current target. When writing restored C, prefer calling the underlying target directly instead of recreating wrapper layers just for symmetry.
- When you rename a label, keep the original `LAB_xxxx` symbol in place and add the new alias directly above it. This preserves binary parity while growing readable names.
- Record noteworthy alias work and outstanding anonymous labels in [`AGENTS.md`](AGENTS.md) so future passes know where to continue.
- Follow the inline documentation template in `AGENTS.md` for function headers, symbol blocks, and struct offsets; keep notes in code rather than new markdown docs.
- Update adjacent data tables (e.g., entries in `src/data/wdisp.s`) whenever you adjust presets or highlight behavior inside `src/modules/gcommand.s`.
- Re-run `./test-hash.sh` after each change; treat hash deltas as regressions unless intentionally diverging.

## Decomp Quick Start
1. Pick a bounded target from `src/decomp/TARGETS.md` or an existing compare/promote script under `src/decomp/scripts/`.
2. If the target already has a `src/decomp/sas_c/*.c` file, build it with `./sc-build-with-dis.sh <file>.c` and inspect the resulting `.dis`.
3. If the target only exists as a GCC lane, use the replacement candidate in `src/decomp/c/replacements/` and its compare script as a behavior reference, then create or fold that logic into the SAS/C lane.
4. Validate with the target-specific compare script first, then run broader gates such as `./test-hash.sh` when the change affects the hybrid/main build path.

## Recent Work
- Completed a full gcommand sweep: all routines now have `GCOMMAND_*` aliases, header blocks, and descriptive local labels (banner/preset pipeline included).
- Banner helpers now include `GCOMMAND_BuildBannerTables`, `GCOMMAND_RebuildBannerTablesFromBounds`, and `GCOMMAND_ServiceHighlightMessages`; see `CONTEXT.md` for the latest rename list.
- Moved `KYBD_UpdateHighlightState` into `src/modules/ladfunc.s` as `LADFUNC_UpdateHighlightState`.
- Added semantic aliases in `src/data/wdisp.s` for Digital Niche/Mplex option-state globals formerly referenced as raw `22D*`/`22E*` offsets (e.g., `GCOMMAND_Niche*` / `GCOMMAND_Mplex*` symbols).
- Renamed local control-flow labels in `src/modules/groups/a/a/bitmap.s` (`BITMAP_ProcessIlbmImage`) and cleaned up NEWGRID local dispatch labels in `src/modules/groups/b/a/newgrid.s` / `src/modules/groups/b/a/newgrid1.s`.
- Seeded `modules/groups/a/a/app.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the APP serial/control helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/a/app3.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the APP3 reboot/init helper module has full non-`JMPTBL` SAS/C compare coverage.
- Converted `modules/groups/a/a/app3.s` from a passthrough boundary to a direct object-level hybrid replacement now that the APP3 reboot/init helper module has green restored SAS/C compare coverage for `ESQ_InvokeGcommandInit`, `ESQ_SupervisorColdReboot`, and `ESQ_TryRomWriteTest`.
- Converted `modules/groups/_main/a/a.s` from a passthrough boundary to a direct object-level hybrid replacement now that the primary `_main` startup/shutdown helper module has green SAS/C compare coverage for `ESQ_StartupEntry`, `ESQ_ReturnWithStackCode`, and `ESQ_ShutdownAndReturn`.
- Seeded `modules/groups/a/d/cleanup3.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the CLEANUP3 aligned-status banner/screen helper module has green restored SAS/C compare coverage.
- Converted `modules/groups/a/d/cleanup3.s` from a passthrough boundary to a direct object-level hybrid replacement now that the CLEANUP3 aligned-status banner/screen helper module has green restored SAS/C compare coverage for `CLEANUP_BuildAndRenderAlignedStatusBanner`.
- Seeded `modules/groups/a/k/ed1.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the ED1 ESC-menu helper module has a green restored SAS/C compare foothold for `ED1_ClearEscMenuMode`.
- Seeded `modules/groups/a/l/ed3.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the ED3 draw-helper module has green restored SAS/C compare footholds for `SET_A_PEN_1_B_PEN_6_DRMD_1_DRAW_TEXT_OR_CURSOR` and `SET_A_PEN_1_B_PEN_6_DRMD_1_DRAW_LINE_OR_PAGE`.
- Seeded `modules/groups/a/m/esq.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the ESQ main startup/init module has a green restored SAS/C compare foothold for `ESQ_MainInitAndRun`.
- Converted `modules/groups/a/m/esq.s` from a passthrough boundary to a direct object-level hybrid replacement now that the ESQ main startup/init module has a green restored SAS/C compare foothold for `ESQ_MainInitAndRun`.
- Seeded `modules/groups/a/s/gcommand.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the main GCOMMAND parser/template helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/t/gcommand2.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the GCOMMAND2 helper foothold (`GCOMMAND_CopyGfxToWorkIfAvailable`, `GCOMMAND_FindPathSeparator`) has green restored SAS/C compare coverage.
- Seeded `modules/groups/a/u/gcommand3.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the GCOMMAND3 banner/highlight helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/u/gcommand4.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the GCOMMAND4 brush-result helper module has a green restored SAS/C compare foothold for `GCOMMAND_SaveBrushResult`.
- Converted `modules/groups/a/v/gcommand5.s` from a passthrough boundary to a direct object-level hybrid replacement now that the GCOMMAND5 control-command helper module has a green restored SAS/C compare foothold for `GCOMMAND_ProcessCtrlCommand`.
- Converted `modules/groups/a/v/kybd.s` from a passthrough boundary to a direct object-level hybrid replacement now that the KYBD input-device initialization module has a green restored SAS/C compare foothold for `KYBD_InitializeInputDevices`.
- Seeded `modules/groups/a/h/diskio2.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the DISKIO2 data-file transfer, sync, and ini/oinfo helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/i/displib.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the DISPLIB text/layout helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/n/esqfunc.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the ESQFUNC status/clock/UI-service helper module has strong restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/n/esqiff.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the ESQIFF external-asset/weather/copper helper module has strong restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/o/esqpars.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the ESQPARS parser/state-persistence helper module has the strongest remaining unmapped non-`JMPTBL` SAS/C foothold in the current checkout.
- Seeded `modules/groups/a/p/esqshared.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the ESQSHARED entry/default/title-filter helper module has a useful restored non-`JMPTBL` SAS/C foothold in the current checkout.
- Seeded `modules/groups/a/r/flib.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the FLIB log-append helper module's restored non-`JMPTBL` SAS/C compare lanes are green in the current checkout.
- Seeded `modules/groups/a/x/ladfunc2.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the LADFUNC2 escaped-string helper module has restored SAS/C coverage in the current checkout.
- Seeded `modules/groups/a/w/ladfunc.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the LADFUNC text-ad/highlight helper module has broad restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/y/locavail.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the LOCAVAIL local-availability filter helper module has strong restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/a/z/locavail2.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the LOCAVAIL2 intuition override helper module has green restored SAS/C compare coverage for `LOCAVAIL2_AutoRequestNoOp` and `LOCAVAIL2_DisplayAlertDelayAndReboot`.
- Seeded `modules/groups/b/a/parseini.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the PARSEINI config/weather/font helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/parseini2.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the PARSEINI2 clock/date/RTC helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/p_type.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the P_TYPE promo-id/group-list helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/script.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the SCRIPT buffer allocation/token-index helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/tliba1.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the TLIBA1 formatted-text/clock-format helper module has useful restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/tliba2.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the TLIBA2 broadcast-window/time-parse helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/tliba3.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the TLIBA3 view-mode/pattern-table helper module has broad restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/newgrid1.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the NEWGRID1 grid drawing/scan helper module has broad restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/textdisp.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the TEXTDISP source-config/selection helper module has broad restored non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/textdisp2.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the TEXTDISP2 preview/display-state helper module has restored non-`JMPTBL` SAS/C compare coverage across its current direct helpers.
- Seeded `modules/groups/b/a/script4.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the SCRIPT4 banner-char/inset/highlight helper module has full non-`JMPTBL` SAS/C compare coverage.
- Seeded `modules/groups/b/a/textdisp3.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the TEXTDISP3 text/layout helper module has a useful restored non-`JMPTBL` SAS/C foothold in the current checkout.
- Seeded `modules/submodules/unknown40.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN40 battery-clock / DOS wrapper helper module has green restored SAS/C compare coverage in the current checkout.
- Seeded `modules/submodules/unknown15.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN15 stream line-reader helper module has green restored SAS/C compare coverage for `STREAM_ReadLineWithLimit` in the current checkout.
- Seeded `modules/submodules/unknown5.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN5 string helper module has green restored SAS/C compare coverage in the current checkout (`STRING_AppendN`, `STRING_CompareN`, `STRING_CompareNoCase`, `STRING_CompareNoCaseN`, and `STRING_CopyPadNul`).
- Seeded `modules/submodules/unknown.s`, `modules/submodules/unknown2a.s`, and `modules/submodules/unknown30.s` in `src/decomp/replacements.map` with passthrough hybrid boundaries now that their restored SAS/C footholds are present in the current checkout (`ESQPROTO_CopyLabelToGlobal`, `ESQPROTO_ParseDigitLabelAndDisplay`, `ESQPROTO_VerifyChecksumAndParseList`, `ESQPROTO_VerifyChecksumAndParseRecord`, `UNKNOWN_ParseListAndUpdateEntries`, `UNKNOWN_ParseRecordAndUpdateDisplay`, `FORMAT_RawDoFmtWithScratchBuffer`, `UNKNOWN2A_Stub0`, and `EXEC_CallVector348`).
- Seeded `modules/submodules/unknown6.s`, `unknown8.s`, `unknown9.s`, `unknown12.s`, `unknown16.s`-`unknown20.s`, `unknown23.s`, `unknown26.s`, `unknown28.s`, `unknown31.s`, `unknown35.s`, `unknown37.s`, and `unknown39.s` in `src/decomp/replacements.map` with passthrough hybrid boundaries now that their restored SAS/C single-helper lanes are green in the current checkout (`STRING_AppendAtNull`, `FORMAT_U32ToDecimalString`, `FORMAT_U32ToOctalString`, `ALLOC_AllocFromFreeList`, `BUFFER_FlushAllAndCloseWithCode`, `DOS_WriteWithErrorState`, `DOS_SeekWithErrorState`, `DOS_ReadWithErrorState`, `DOS_OpenWithErrorState`, `HANDLE_GetEntryByIndex`, `DOS_WriteByIndex`, `WDISP_FormatWithCallback`, `BUFFER_EnsureAllocated`, `HANDLE_OpenWithMode`, `HANDLE_CloseByIndex`, and `GRAPHICS_BltBitMapRastPort`).
- Seeded `modules/submodules/unknown10.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN10 format/parse/open helper module has a useful restored SAS/C foothold in the current checkout (`FORMAT_U32ToHexString`, `PARSE_ReadSignedLong`, `PARSE_ReadSignedLong_NoBranch`, `HANDLE_OpenEntryWithFlags`, `UNKNOWN10_PrintfPutcToBuffer`, `WDISP_SPrintf`).
- Seeded `modules/submodules/unknown13.s`, `modules/submodules/unknown25.s`, `modules/submodules/unknown27.s`, `modules/submodules/unknown33.s`, and `modules/submodules/unknown36.s` in `src/decomp/replacements.map` with passthrough hybrid boundaries now that their GCC promotion lanes are green in the current checkout (`FORMAT_CallbackWriteChar` / `FORMAT_FormatToCallbackBuffer`, `STRUCT_AllocWithOwner` / `STRUCT_FreeWithSizeField`, `FORMAT_Buffer2WriteChar` / `FORMAT_FormatToBuffer2`, `ALLOC_InsertFreeBlock` / `STRING_FindSubstring`, and `UNKNOWN36_FinalizeRequest` / `UNKNOWN36_ShowAbortRequester`).
- Seeded `modules/submodules/unknown4.s`, `modules/submodules/unknown11.s`, and `modules/submodules/unknown14.s` in `src/decomp/replacements.map` with passthrough hybrid boundaries now that the restored SAS/C single-helper lanes for `STRING_ToUpperInPlace`, `DOS_SeekByIndex`, and `HANDLE_OpenFromModeString` are green in the current checkout.
- Converted `modules/submodules/unknown4.s` from a passthrough boundary to a direct object-level hybrid replacement now that the UNKNOWN4 uppercase helper module has green restored SAS/C compare coverage in the current checkout (`STRING_ToUpperInPlace`).
- Converted `modules/submodules/unknown11.s` from a passthrough boundary to a direct object-level hybrid replacement now that the UNKNOWN11 DOS seek helper module has green restored SAS/C compare coverage in the current checkout (`DOS_SeekByIndex`).
- Converted `modules/submodules/unknown14.s` from a passthrough boundary to a direct object-level hybrid replacement now that the UNKNOWN14 handle-open helper module has green restored SAS/C compare coverage in the current checkout (`HANDLE_OpenFromModeString`).
- Seeded `modules/submodules/unknown21.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN21 DOS/file-create and IOStdReq cleanup helper module has green restored SAS/C compare coverage in the current checkout (`IOSTDREQ_Free`, `IOSTDREQ_CleanupSignalAndMsgport`, `DOS_OpenNewFileIfMissing`, `DOS_DeleteAndRecreateFile`).
- Seeded `modules/submodules/unknown22.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN22 arithmetic/msgport helper module has green restored SAS/C compare coverage in the current checkout (`ALLOCATE_AllocAndInitializeIOStdReq`, `DOS_CloseWithSignalCheck`, `MATH_DivS32`, `MATH_DivU32`, `MATH_Mulu32`, `SIGNAL_CreateMsgPortWithSignal`).
- Seeded `modules/submodules/unknown24.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN24 memlist/signed-long helper module has green restored SAS/C compare coverage in the current checkout (`MEMLIST_AllocTracked`, `MEMLIST_FreeAll`, `PARSE_ReadSignedLongSkipClass3`, `PARSE_ReadSignedLongSkipClass3_Alt`).
- Seeded `modules/submodules/unknown29.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN29 command-line/startup helper module has green restored SAS/C compare coverage in the current checkout (`ESQ_ParseCommandLineAndRun`).
- Seeded `modules/submodules/unknown32.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN32 handle-close/return helper module has green restored SAS/C compare coverage in the current checkout (`HANDLE_CloseAllAndReturnWithCode`).
- Seeded `modules/submodules/unknown34.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN34 list/memory/read helper module has green restored SAS/C compare coverage in the current checkout (`LIST_InitHeader`, `MEM_Move`, `DOS_ReadByIndex`).
- Converted `modules/submodules/unknown38.s` from a passthrough boundary to a direct object-level hybrid replacement now that the UNKNOWN38 signal/exit helper module has green restored SAS/C compare coverage in the current checkout (`SIGNAL_PollAndDispatch`).
- Seeded `modules/submodules/unknown7.s` in `src/decomp/replacements.map` with a passthrough hybrid boundary now that the UNKNOWN7 string delimiter/search helper module has green restored SAS/C compare coverage in the current checkout.

## Contributor Guide
If you plan to extend or annotate the disassembly, read the contributor guidelines in [`AGENTS.md`](AGENTS.md) for module organization, coding style, testing expectations, and review workflow.

## Extra References
- DST file (`df0:dst.dat`) format that dictates daylight savings time start/stop: [`docs/dst-dat-format.md`](docs/dst-dat-format.md)
- LocAvail file (`DF0:LocAvail.dat`) token-stream format used by local availability filters: [`docs/locavail-dat-format.md`](docs/locavail-dat-format.md)
- Config file (`df0:config.dat`) fixed-order serialized layout: [`docs/config-dat-format.md`](docs/config-dat-format.md)
- Programming payload files (`df0:curday.dat`, `df0:nxtday.dat`) layout: [`docs/curday-nxtday-dat-format.md`](docs/curday-nxtday-dat-format.md)
- OI metadata file (`df0:oinfo.dat`) token layout: [`docs/oinfo-dat-format.md`](docs/oinfo-dat-format.md)
- Qtable alias export (`df0:qtable.ini`) format: [`docs/qtable-ini-format.md`](docs/qtable-ini-format.md)
- Promo type file (`df0:PromoId.Dat`) sectioned text format: [`docs/promoid-dat-format.md`](docs/promoid-dat-format.md)
- Source config file (`df0:SourceCfg.ini`) key/value format: [`docs/sourcecfg-ini-format.md`](docs/sourcecfg-ini-format.md)
- Gradient preset file (`df0:Gradient.ini`) range-table format: [`docs/gradient-ini-format.md`](docs/gradient-ini-format.md)
- Parser fixture samples (escaped) for `config.dat` and `LocAvail.dat`: [`docs/examples/README.md`](docs/examples/README.md)
- File-format doc progress tracker: [`docs/file-format-status.md`](docs/file-format-status.md)
